  - platform: generic_thermostat
    name: Heater
    heater: switch.heater_switch
    target_sensor: sensor.average_temperature
    min_temp: 16
    max_temp: 22
    cold_tolerance: 0.5
    hot_tolerance: 0.0
    away_temp: 16 # has to be set from here
    precision: 0.5
    initial_hvac_mode: "heat"
    min_cycle_duration:
      minutes: 10
